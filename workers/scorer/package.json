{
  "name": "@b2bcorpcom/worker-scorer",
  "version": "0.0.0",
  "private": true,
  "description": "Scorer Worker — evaluates candidate companies for 'cleanly basic' onboard-media (PLAN.md D13).",
  "scripts": {
    "dev": "echo 'TODO(week 3): wrangler dev'",
    "build": "echo 'TODO(week 3): wrangler build'",
    "deploy": "echo 'TODO(week 3): wrangler deploy'",
    "typecheck": "echo 'TODO(week 3): tsc --noEmit'",
    "test": "echo 'no tests yet'"
  }
}
