{
  "name": "@b2bcorpcom/worker-normalizer",
  "version": "0.0.0",
  "private": true,
  "description": "Normalizer Worker — converts crawled HTML to cleaned text + metadata.",
  "scripts": {
    "dev": "echo 'TODO(week 6): wrangler dev'",
    "build": "echo 'TODO(week 6): wrangler build'",
    "deploy": "echo 'TODO(week 6): wrangler deploy'",
    "typecheck": "echo 'TODO(week 6): tsc --noEmit'",
    "test": "echo 'no tests yet'"
  }
}
