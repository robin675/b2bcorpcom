# @b2bcorpcom/worker-discovery

Cloudflare Worker (cron). 검색 API를 호출해 "한국 중견 B2B 제조·부품·소재" 후보 회사 URL을 발굴, `candidate` 테이블에 적재한다.

방향 키워드는 `config` 테이블에서 GUI로 변경 가능해야 함 (CLAUDE.md §4.2).

v0 2주차 스캐폴딩 예정.
