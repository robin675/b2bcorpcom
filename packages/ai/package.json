{
  "name": "@b2bcorpcom/ai",
  "version": "0.0.0",
  "private": true,
  "description": "Claude API client + prompt modules (scorer / normalizer / embedder inputs).",
  "scripts": {
    "typecheck": "echo 'TODO(week 3): tsc --noEmit'",
    "test": "echo 'no tests yet'"
  }
}
