{
  "name": "@b2bcorpcom/shared",
  "version": "0.0.0",
  "private": true,
  "description": "Shared types and utilities (URL normalization, logging, etc).",
  "scripts": {
    "typecheck": "echo 'TODO(week 1): tsc --noEmit'",
    "test": "echo 'no tests yet'"
  }
}
