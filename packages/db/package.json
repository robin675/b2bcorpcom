{
  "name": "@b2bcorpcom/db",
  "version": "0.0.0",
  "private": true,
  "description": "Supabase types for the v0 pipeline.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "gen-types": "echo 'For now, types are hand-written in src/database.types.ts; refresh with the Supabase MCP generate_typescript_types tool.'",
    "typecheck": "tsc --noEmit",
    "test": "echo 'no tests yet'"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
