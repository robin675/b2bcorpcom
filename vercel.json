{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "buildCommand": "pnpm --filter @b2bcorpcom/admin build",
  "installCommand": "pnpm install --frozen-lockfile",
  "outputDirectory": "apps/admin/.next",
  "ignoreCommand": "git diff --quiet HEAD^ HEAD ':(exclude)workers/' ':(exclude)packages/ai/' ':(exclude)packages/shared/' ':(exclude)supabase/'"
}
