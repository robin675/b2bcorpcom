{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "pnpm --filter @b2bcorpcom/admin build && mkdir -p apps/admin && rm -rf apps/admin/.next && mv .next apps/admin/.next",
  "installCommand": "pnpm install --frozen-lockfile"
}
